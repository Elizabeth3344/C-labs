#ifndef FUNC_H // проверяем, не был оли определен этот файл ранее
#define FUNC_H // определяем файл, если выполнилось условие; если нет - пропускает код

float my_func(float x); // функция принимает на вход аргумент типа float и возвращает float

#endif // завершение условия ifndef
