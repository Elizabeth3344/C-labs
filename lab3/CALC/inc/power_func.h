#ifndef POWER_H // проверяем, не был оли определен этот файл ранее
#define POWER_H // определяем файл, если выполнилось условие; если нет - пропускает код

double power(double base, int exponent);

#endif // завершение условия ifndef