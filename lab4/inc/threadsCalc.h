#ifndef THREADS_CALC_H // проверяем, не был оли определен этот файл ранее
#define THREADS_CALC_H // определяем файл, если выполнилось условие; если нет - пропускает код

void threadsCalc(int iterations);

#endif // завершение условия ifndef