#ifndef PROCESSES_CALC_H // проверяем, не был оли определен этот файл ранее
#define PROCESSES_CALC_H // определяем файл, если выполнилось условие; если нет - пропускает код

void processesCalc(int iterations);

#endif // завершение условия ifndef